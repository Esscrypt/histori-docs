name: Deploy Docusaurus site

on:
  push:
    branches:
      - main  # Adjust this to the branch you want to deploy from

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install latest Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 'node'  # This will automatically use the latest Node.js version

      - name: Install dependencies
        run: npm install

      - name: Build the site
        run: npm run build

      - name: Deploy to GitHub Pages
        run: npx docusaurus deploy
        env:
          GIT_USER: ${{ secrets.GIT_USER }}  # Set up GIT_USER in your GitHub secrets
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
